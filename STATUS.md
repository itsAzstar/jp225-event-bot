# 部署現況

**Repo**：https://github.com/itsAzstar/jp225-event-bot （公開）
**排程**：每 5 分鐘（GitHub Actions）
**chat_id**：`921775047`（已自動偵測並 commit 回 repo，永久生效）
**狀態**：✅ 上線運作中

---

## 已驗證項目

| 項目 | 結果 |
|---|---|
| Token 有效 | ✅ getMe 200 |
| Token 存入 GitHub Secrets | ✅ |
| 執行紀錄中 token 遮蔽 | ✅ 出現 0 次 |
| repo 內含 token 的檔案 | ✅ 0 個 |
| workflow YAML | ✅ 解析正確 |
| 手動觸發 | ✅ 成功 |
| 排程自動執行 | ✅ 已確認（16:16 UTC 那次） |
| chat_id 自動偵測 | ✅ 已發現並寫回 repo |
| Telegram 實際送達 | ✅ 已送出並確認 |
| 取價正確性 | ✅ `^N225` = 65,606.71，與 TradingView 一致 |
| 過期報價保護 | ✅ 超過 20 分鐘回 None |
| 重複問候防護 | ✅ 已修（見下方） |

---

## 修過的 bug

### 每 5 分鐘重發問候訊息（一天 288 則）

**成因**：去重旗標寫在本地檔 `chat_id.txt.greeted`，但 GitHub Actions
每次執行都是全新容器，本地檔不會留存 → 每次都判定為「首次」。

**修法**：改用 `NEWLY_FOUND` 旗標，只在「本次才從 getUpdates 發現
chat_id」時問候。`chat_id.txt` 有 commit 回 repo，後續執行走
「由檔案取得」路徑，不觸發問候。

**驗證**：本地連跑三次 + GitHub Actions 一次，確認第二次之後
telegram 送出次數為 0。

---

## ⚠️ 兩件只有你能做

### 1. 更換 token

這組 token 曝光在對話紀錄裡，任何人拿到都能冒充你的 bot。

1. @BotFather → `/revoke` → 選 azstartest_bot
2. GitHub repo → Settings → Secrets → 更新 `TELEGRAM_BOT_TOKEN`

repo 內無任何檔案含 token（已掃描確認），但曝光就該換。

### 2. 改 bot 名稱

`getMe` 顯示 `first_name` 是一個種族蔑稱。@BotFather → `/setname`。
若要對外販售，這個名稱會直接毀掉產品。

---

## GitHub Actions 的三個真實限制

1. **排程延遲 5～15 分鐘**（免費層已知行為）。「事件前 10 分鐘提醒」
   實際可能變成事件當下才到。
2. **repo 60 天無 commit 會自動停用排程**，需手動重啟。
3. **偶爾漏跑**，GitHub 不保證 cron 必定執行。

需要準時到分鐘 → 見 `DEPLOY.md` 方案 B（Oracle Cloud 永久免費 VM）。

---

## 這個工具能做什麼、不能做什麼

**能**：告訴你哪些時段波動會放大（跨 2021 / 2023 / 2026 三個制度驗證形狀一致），
用來決定停損寬度與是否值得盯盤。

**不能**：預測漲跌。方向類訊號在本專案實測中全數失敗：

| 測試 | 結果 |
|---|---|
| 13:00 時段訊號 | 2021 虧 1,279 點、2023 虧 726 點 |
| 5 分趨勢跟隨 | 2026 賺 34.5%，2021 零成本都虧 |
| M1 反轉 74 種組合 | 扣成本後 0 種獲利 |
| 雜訊對照 | 13:00 的每項「證據」純隨機都能複製 |

**因此不應包裝成訊號服務或獲利工具對外販售。**
多數司法管轄區販售投資建議需要證照。

---

## 新聞 AI 摘要（2026-08-10 加入）

**來源**：Google News RSS（免金鑰）
**摘要**：DeepSeek `deepseek-chat`
**觸發**：每日摘要時間（預設 07:00 台北）一併發送

三道防幻覺機制見 `ANTI_HALLUCINATION.md`：
1. 強制每句標註來源標題編號 `(#N)`，訊息附編號對照可查核
2. 二次查核 pass（temperature=0）刪除無依據說法
3. 程式強制刪除無 `(#N)` 的句子 + 數字必須出現在標題中

任一步失敗一律回 None，不發送也不編造。

**已實測**：上週五（08/07）10 則標題 → 摘要正確，
三道檢核全過（查核無修改、刪除 0 句、幽靈數字 0 個），已送達 Telegram。
