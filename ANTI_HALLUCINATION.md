# 防幻覺機制

新聞摘要由 LLM 產生，LLM 會編造。這份文件說明我們用什麼擋，以及擋不住什麼。

## 架構

```
Google News RSS  →  標題（免金鑰）
      ↓
DeepSeek 第一次  →  摘要草稿
      ↓
DeepSeek 第二次  →  自我查核，刪除無依據說法
      ↓
Python 程式      →  強制刪除無來源編號的句子 + 數字比對
      ↓
Telegram
```

## 三道防線

### 1. 強制標註來源編號

system prompt 要求每一行都必須標 `(#N)` 對應標題編號。
訊息底部附上編號對照的原始標題，**你可以逐條查核**。

實例：摘要寫「軟銀集團跌幅超過2% (#6)(#10)」，
查 `#6` = "SoftBank Falls Over 2%"、`#10` = "SoftBank Falls Over 5%" → 屬實。

### 2. 二次查核 pass

把「原始標題 + 摘要草稿」再送回模型，角色改成事實查核員，
逐句檢查是否有依據，沒依據的整句刪除。`temperature=0`。

### 3. 程式強制過濾

- 任何以 `-` 開頭或 `【整體偏向】` 開頭的句子，**沒有 `(#N)` 就直接刪除**
- 摘要中出現的數字必須能在原始標題中找到，否則列入 `ghost_numbers` 警告

第 3 道是唯一不依賴模型自律的防線。

## ⚠️ 擋不住什麼

### 語言不同導致無法做實體比對

標題是英文、摘要是中文。「SoftBank」對「軟銀集團」無法用字串比對驗證。
所以我們**沒有**辦法程式化驗證公司名、事件名是否被正確對應 ——
只能靠來源編號讓人工查核。

### 標題本身就是錯的

新聞標題若有誤，摘要會忠實地傳遞這個錯誤。本系統不驗證新聞真實性。

### 標題被截斷

Google News RSS 給的是完整標題，但**顯示時若截斷，人工查核會誤判**。

> 開發過程中我曾把顯示截斷在 80 字元，用截斷版比對後
> 誤判模型「編造」了家庭支出與美國就業數據 —— 實際上兩者都在
> 完整標題裡（`#4` 結尾是 "Household Spending Falls for 7th Straight Month"、
> `#3` 結尾是 "Ahead of U.S. Jobs Data"）。
> **查核時務必看完整標題。**

## 失敗時的行為

任何一步失敗（RSS 抓不到、API 401、逾時），一律回 `None`，
**不發送、不編造替代內容**。寧可沒訊息，不要有假訊息。

## 成本

每次分析約 800～900 tokens（兩次呼叫合計）。
每日只在早上摘要時跑一次。DeepSeek 定價下每月不到 0.1 美元。
